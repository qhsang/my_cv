import React from 'react';
import '../App.css';

const Header: React.FC = () => {
    return (
        <header className="App-header">
            <h1>My CV</h1>
        </header>
    );
};

export default Header;